<%= render 'list' %>

<div id="main" class="pane">
  <h3 class="heading">
    <%= t('dashboard.posts.new.header', resource: @post.human_name) %>
  </h3>
  <div class="scroll-y">
    <div class="padding">
      <%= render 'form' %>
    </div>
  </div>
</div>