<%= render 'storytime/dashboard/navigation' if user_signed_in? && !devise_controller? && current_user.storytime_user? %>

<% unless dashboard_controller %>
  <%= link_to "#", class: "btn btn-primary btn-sm pull-left", data: { toggle: "off-canvas",  target: "#dashboard-nav" }, style: "position: fixed; top: 10px; left: 20px; z-index: 1030;" do %>
    Storytime
  <% end %>
<% end %>