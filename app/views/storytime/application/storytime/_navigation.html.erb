<% if user_signed_in? && !devise_controller? && current_user.storytime_user? %>
  <%= render 'storytime/dashboard/navigation' %>
  <%= link_to "Storytime", "#", class: "btn btn-primary btn-sm pull-left", data: { toggle: "off-canvas",  target: "#dashboard-nav" }, style: "position: fixed; top: 10px; left: 15px; z-index: 1030;" unless dashboard_controller %>
<% end %>